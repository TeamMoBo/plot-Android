<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.mobo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".movie_selection.display.MovieSelectionActivity"/>
        <activity android:name=".mypage.MyPage_new"></activity>
        <activity android:name=".MatchingDetailedActivity"/>
        <activity android:name=".MatchingHistory.MatchingHistoryActivity" ></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".main_page.MainPageActivity" />
        <activity android:name=".sign.SignUpSuccessActivity" />
        <activity android:name=".sign.SignUpPlusActivity" />
        <activity android:name=".sign.SignUpBasicActivity" />

        <activity android:name=".sign.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chat.ChattingActivity" />
        <activity android:name=".PayChoiceActivity" />
        <activity android:name=".time_choice.TimeChoiceActivity" />
        <activity android:name=".temp.TempActivity">
        </activity>

    </application>
</manifest>